# Loaded automatically by `npm run build` (Vite mode=production).
# The Django backend isn't served from the same origin as this app, so the
# API base URL must be absolute here (unlike dev, which rides the Vite proxy).
VITE_API_BASE_URL=https://saudidashboard.pythonanywhere.com/api
